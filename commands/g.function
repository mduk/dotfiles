function g {
    grep --color=always \
         --exclude=*.swp \
         --exclude=*.pyc \
         --exclude-dir=.git/ \
         --recursive \
         --line-number \
         "$1" \
         "${2:-.}"
}

#!/bin/bash

janet=192.168.0.34
key='janet'
keypath="$HOME/.ssh/${key}_rsa"

arg_key="-i $keypath"

case "$1" in
  push) scp $arg_key $2 "$USER@${janet}:/home/$USER/Pushed/$2" ;;
  *) ssh -XA $arg_key "$USER@${janet}" ;;
esac

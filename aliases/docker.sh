# Docker Aliases

# Docker Exec: Run a command within a container
alias de="docker exec"

# Crane Lift: Make ALL THE THINGS be running.
#   This will walk up the directory tree until it finds a crane.yml file
alias cl="crane lift"

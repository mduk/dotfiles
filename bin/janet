#!/bin/bash

janet=192.168.0.34
key='janet'
keypath="$HOME/.ssh/${key}_rsa"

arg_key="-i $keypath"

case "$1" in
  push) rsync -avh "$2" "$USER@$janet:$2" ;;
  pull) rsync -avh "$USER@$janet:$2" "$2" ;;
  *) ssh -XA $arg_key "$USER@$janet" "$@";;
esac

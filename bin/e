#!/bin/bash

path="$1"

if [ -f "$path" ]
then
  owner="$(ls -ld "$path" | awk '{print $3}')"

  if [[ "$owner" != "$USER" ]]
  then
    sudo $EDITOR "$path"
  else
    $EDITOR "$path"
  fi
else
  $EDITOR "$path"
fi
